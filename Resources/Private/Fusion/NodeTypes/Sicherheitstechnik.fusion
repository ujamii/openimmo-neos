prototype(Ujamii.OpenImmo:Content.Sicherheitstechnik) < prototype(Neos.Fusion:Component) {
    alarmanlage = ${q(node).property('alarmanlage')}
    kamera = ${q(node).property('kamera')}
    polizeiruf = ${q(node).property('polizeiruf')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Sicherheitstechnik {...props}/>
    `
}