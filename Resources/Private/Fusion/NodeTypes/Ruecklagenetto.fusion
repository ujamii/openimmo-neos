prototype(Ujamii.OpenImmo:Content.Ruecklagenetto) < prototype(Neos.Fusion:Component) {
    ruecklageust = ${q(node).property('ruecklageust')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Ruecklagenetto {...props}/>
    `
}