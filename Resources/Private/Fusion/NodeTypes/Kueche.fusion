prototype(Ujamii.OpenImmoNeos:Content.Kueche) < prototype(Neos.Fusion:Component) {
    ebk = ${q(node).property('ebk')}
    offen = ${q(node).property('offen')}
    pantry = ${q(node).property('pantry')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.Kueche {...props}/>
    `
}