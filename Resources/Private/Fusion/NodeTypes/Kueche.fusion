prototype(Ujamii.OpenImmo:Content.Kueche) < prototype(Neos.Fusion:Component) {
    ebk = ${q(node).property('ebk')}
    offen = ${q(node).property('offen')}
    pantry = ${q(node).property('pantry')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Kueche {...props}/>
    `
}