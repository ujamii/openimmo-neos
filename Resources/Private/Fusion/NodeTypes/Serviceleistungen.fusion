prototype(Ujamii.OpenImmoNeos:Content.Serviceleistungen) < prototype(Neos.Fusion:Component) {
    betreutesWohnen = ${q(node).property('betreutesWohnen')}
    catering = ${q(node).property('catering')}
    einkauf = ${q(node).property('einkauf')}
    reinigung = ${q(node).property('reinigung')}
    wachdienst = ${q(node).property('wachdienst')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.Serviceleistungen {...props}/>
    `
}