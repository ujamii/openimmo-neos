prototype(Ujamii.OpenImmoNeos:Content.StpParkhaus) < prototype(Neos.Fusion:Component) {
    anzahl = ${q(node).property('anzahl')}
    stellplatzkaufpreis = ${q(node).property('stellplatzkaufpreis')}
    stellplatzmiete = ${q(node).property('stellplatzmiete')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.StpParkhaus {...props}/>
    `
}