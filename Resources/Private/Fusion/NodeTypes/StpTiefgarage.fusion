prototype(Ujamii.OpenImmo:Content.StpTiefgarage) < prototype(Neos.Fusion:Component) {
    anzahl = ${q(node).property('anzahl')}
    stellplatzkaufpreis = ${q(node).property('stellplatzkaufpreis')}
    stellplatzmiete = ${q(node).property('stellplatzmiete')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.StpTiefgarage {...props}/>
    `
}