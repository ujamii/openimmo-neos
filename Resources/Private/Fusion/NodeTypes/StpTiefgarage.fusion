prototype(Ujamii.OpenImmoNeos:Content.StpTiefgarage) < prototype(Neos.Fusion:Component) {
    anzahl = ${q(node).property('anzahl')}
    stellplatzkaufpreis = ${q(node).property('stellplatzkaufpreis')}
    stellplatzmiete = ${q(node).property('stellplatzmiete')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.StpTiefgarage {...props}/>
    `
}