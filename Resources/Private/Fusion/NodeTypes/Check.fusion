prototype(Ujamii.OpenImmo:Content.Check) < prototype(Neos.Fusion:Component) {
    ctype = ${q(node).property('ctype')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Check {...props}/>
    `
}