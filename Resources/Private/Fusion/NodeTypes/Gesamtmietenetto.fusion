prototype(Ujamii.OpenImmoNeos:Content.Gesamtmietenetto) < prototype(Neos.Fusion:Component) {
    gesamtmieteust = ${q(node).property('gesamtmieteust')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.Gesamtmietenetto {...props}/>
    `
}