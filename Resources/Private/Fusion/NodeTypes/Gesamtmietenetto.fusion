prototype(Ujamii.OpenImmo:Content.Gesamtmietenetto) < prototype(Neos.Fusion:Component) {
    gesamtmieteust = ${q(node).property('gesamtmieteust')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Gesamtmietenetto {...props}/>
    `
}