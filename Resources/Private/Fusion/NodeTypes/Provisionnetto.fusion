prototype(Ujamii.OpenImmo:Content.Provisionnetto) < prototype(Neos.Fusion:Component) {
    provisionust = ${q(node).property('provisionust')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Provisionnetto {...props}/>
    `
}