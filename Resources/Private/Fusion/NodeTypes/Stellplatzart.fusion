prototype(Ujamii.OpenImmoNeos:Content.Stellplatzart) < prototype(Neos.Fusion:Component) {
    carport = ${q(node).property('carport')}
    duplex = ${q(node).property('duplex')}
    freiplatz = ${q(node).property('freiplatz')}
    garage = ${q(node).property('garage')}
    parkhaus = ${q(node).property('parkhaus')}
    tiefgarage = ${q(node).property('tiefgarage')}
    renderer = afx`
        <Ujamii.OpenImmoNeos:Component.Molecule.Stellplatzart {...props}/>
    `
}