prototype(Ujamii.OpenImmo:Content.StpSonstige) < prototype(Neos.Fusion:Component) {
    bemerkung = ${q(node).property('bemerkung')}
    platzart = ${q(node).property('platzart')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.StpSonstige {...props}/>
    `
}