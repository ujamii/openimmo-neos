prototype(Ujamii.OpenImmo:Content.Foto) < prototype(Neos.Fusion:Component) {
    daten = ${q(node).property('daten')}
    format = ${q(node).property('format')}
    location = ${q(node).property('location')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.Foto {...props}/>
    `
}