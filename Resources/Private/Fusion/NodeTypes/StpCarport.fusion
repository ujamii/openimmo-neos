prototype(Ujamii.OpenImmo:Content.StpCarport) < prototype(Neos.Fusion:Component) {
    anzahl = ${q(node).property('anzahl')}
    stellplatzkaufpreis = ${q(node).property('stellplatzkaufpreis')}
    stellplatzmiete = ${q(node).property('stellplatzmiete')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.StpCarport {...props}/>
    `
}