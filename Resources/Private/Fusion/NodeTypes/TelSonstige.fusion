prototype(Ujamii.OpenImmo:Content.TelSonstige) < prototype(Neos.Fusion:Component) {
    bemerkung = ${q(node).property('bemerkung')}
    telefonart = ${q(node).property('telefonart')}
    value = ${q(node).property('value')}
    renderer = afx`
        <Ujamii.OpenImmo:Component.Molecule.TelSonstige {...props}/>
    `
}